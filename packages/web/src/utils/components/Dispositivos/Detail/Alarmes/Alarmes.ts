import styled from "styled-components";

const StyledAlarmes = styled.div`
    display: flex;
    align-items: center;
    justify-content: space-evenly;
    flex-direction: column;
`;

export default StyledAlarmes;
