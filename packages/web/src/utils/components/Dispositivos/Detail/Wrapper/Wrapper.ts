import styled from "styled-components";

const Wrapper = styled.div`
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr 0.8fr 58px;
    width: 90%;
    height: 90vh;
`;

export default Wrapper;
